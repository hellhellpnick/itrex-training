import styled from 'styled-components';

const StylArrowFromForm = styled.img`
  position: relative;
  width: 20px;
  height: 20px;
  transform: rotate(180deg) translateY(-10%);
  top: 50%;
  margin-right: 20px;

  z-index: 1;
`;

export default StylArrowFromForm;
