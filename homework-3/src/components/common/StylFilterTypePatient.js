import styled from 'styled-components';

const StylFilterTypePatient = styled.span`
  color: ${(props) => props.theme.palette.text.second};
  margin-left: 10px;
  margin-right: 8px;
`;

export default StylFilterTypePatient;
